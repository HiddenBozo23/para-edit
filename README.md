# para-edit
C++ OpenGL game with an ECS level editor

if building on linux, glfw3 requires the following packages to be installed:
    libxinerama-dev libxcursor-dev xorg-dev libglu1-mesa-dev pkg-config